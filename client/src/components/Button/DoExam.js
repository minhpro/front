import React from "react";
import * as Mui from "@mui/material";
import * as Co from "components";
import styled from "styled-components";

export const DoExam = () => {
  return <Style.Wrapper>DoExam</Style.Wrapper>;
};

const Style = {
  Wrapper: styled.div`
    background-color: var(--color-title);
  `,
};
