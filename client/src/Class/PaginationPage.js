class PaginationPage {
  constructor(func, data, page, total, limit) {
    this.data = data;
    this.page = page;
    this.total = total;
    this.limit = limit;
    this.func = func;
  }

  getData(data, total) {
    this.func;
  }
}
